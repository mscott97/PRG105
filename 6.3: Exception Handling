def main():
    try: numbers_file = open("numbers.txt", "r")
    except Exception as errorGenerated:
        print("file not found:", errorGenerated )
    else:
      total = 0
      number_of_lines = 0
      line = numbersFile.readline()

    while line != "":
        number_of_lines += 1
        total += int(line)
        line = numbers_file.readline()
    average = total / number_of_lines

except as error:
       Print("There has been an IOError: ", error)
except valuerror as error:
       Print("There has been an ValuError: ", error)
  else:
       print("The Average is: ", average)
       print("the total is, total")
       print("the average is", average )
